__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

outputs/

# Local source/downloaded datasets live here; keep only the directory guide tracked.
data/*
!data/README.md

# Local media and third-party tool bundles are documented but not committed.
gs_gen/asset/*.mp4
gs_gen/tools/colmap-4.0.4-nocuda/

# Local CUDA/PyTorch extension caches should not live in the repo.
torch_extensions/
*.egg-info/
